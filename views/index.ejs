<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Trade.web.id - Harga Terkini</title>

  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <style>
    body {
      background: #0d1117;
      color: #e6edf3;
      font-family: "Segoe UI", sans-serif;
      margin: 0;
      padding: 0;
    }

    header {
      padding: 18px;
      background: #161b22;
      box-shadow: 0px 2px 10px rgba(0,0,0,0.4);
      position: sticky;
      top: 0;
      z-index: 999;
    }

    h1 {
      margin: 0;
      font-size: 26px;
      letter-spacing: 1px;
      color: #58a6ff;
      text-align: center;
    }

    .container {
      margin: 40px auto;
      width: 90%;
      max-width: 900px;
      background: #161b22;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(0,0,0,0.4);
      overflow-x: auto;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      min-width: 600px;
    }

    th, td {
      padding: 15px;
      border-bottom: 1px solid #30363d;
      text-align: center;
    }

    th {
      background: #21262d;
      color: #58a6ff;
      font-size: 15px;
    }

    tr:hover td {
      background: rgba(88, 166, 255, 0.05);
    }

    .small {
      font-size: 13px;
      color: #8b949e;
      margin-top: 20px;
      text-align: center;
    }

    @media (max-width: 600px) {
      h1 {
        font-size: 20px;
      }
      th, td {
        padding: 10px;
        font-size: 14px;
      }
    }
  </style>
</head>

<body>

<header>
  <h1>📈 Trade.web.id – Harga Terkini</h1>
</header>

<div class="container">
  <table>
    <tr><th>Asset</th><th>USD</th><th>IDR</th></tr>

    <tr>
      <td>Bitcoin (BTC)</td>
      <td>$<%= prices.btc.usd.toLocaleString() %></td>
      <td>Rp<%= prices.btc.idr.toLocaleString() %></td>
    </tr>

    <tr>
      <td>Ethereum (ETH)</td>
      <td>$<%= prices.eth.usd.toLocaleString() %></td>
      <td>Rp<%= prices.eth.idr.toLocaleString() %></td>
    </tr>

    <tr>
      <td rowspan="2">Gold (XAU)</td>
      <td>$<%= prices.gold.gram_usd.toLocaleString(undefined, {minimumFractionDigits:2}) %> / gram</td>
      <td>Rp<%= prices.gold.gram_idr.toLocaleString() %> / gram</td>
    </tr>
    <tr>
      <td>$<%= prices.gold.ounce_usd.toLocaleString(undefined, {minimumFractionDigits:2}) %> / oz</td>
      <td>Rp<%= prices.gold.ounce_idr.toLocaleString() %> / oz</td>
    </tr>
  </table>

  <p class="small">* Harga mungkin sedikit tertunda sesuai API</p>
</div>

</body>
</html>
